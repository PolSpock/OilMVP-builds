# OilMVP - page de telechargement

Depot de PAGE uniquement. Aucun code source, et surtout **aucun binaire** : l'APK est publiee
dans les releases du depot prive `PolSpock/OilMVP`, que GitHub ne sert qu'a un navigateur
connecte au compte. Un robot qui crawle recoit 404.

Ce depot ne contient que :

| Fichier | Role |
| --- | --- |
| `index.html` | la page, avec le bouton vers la release privee |
| `latest.json` | metadonnees de la derniere build, ecrites par la CI a chaque publication |
| `qr.png` | QR vers la page |
| `robots.txt` | interdit l'indexation |

Page : https://polspock.github.io/OilMVP-builds/
